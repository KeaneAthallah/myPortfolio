import React from "react";
import NavigationBar from "./NavigationBar";

const Header = ({ auth }) => {
    return (
        <div>
            <NavigationBar />
        </div>
    );
};

export default Header;
